version: '3.8'

services:
  portfolio-davi-lucas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio-davi-lucas
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
